[project]
name = "compute_worker"
version = "0.1.0"
description = "Standalone Compute Worker"
requires-python = ">=3.9"
dependencies = [
    "sqlalchemy>=2.0.0",
    "paho-mqtt>=1.6.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
